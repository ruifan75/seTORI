# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/setori?sslmode=disable

# API Keys
HOLODEX_API_KEY=your-holodex-api-key
HOLODEX_EDITOR_TOKEN=your-holodex-editor-token
# GROQ_API_KEY 為「未在管理介面設定任何 AI provider 時」的後備 key。
# 建議改用管理介面（設定頁 → AIプロバイダー管理）登記多個 OpenAI 相容 provider 輪流使用。
GROQ_API_KEY=your-groq-api-key
YOUTUBE_API_KEY=your_api_key_here

# JWT (reserved, not yet used — 認証はDBセッション方式)
JWT_SECRET=your-jwt-secret-change-in-production

# 初期管理者アカウント：ユーザーが 0 件のとき初回起動時に作成される。
# 作成後は管理画面（管理 → ユーザー）からパスワードを変更してください。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# API auth（後方互換）— 設定すると、この静的トークンを Bearer で送れば全権限の管理者として扱われる。
# 通常はアカウントログイン（/api/auth/login）を使う。留空で無効。
API_AUTH_TOKEN=

# Environment
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8080
